name: morning
on:
  schedule:
  - cron:  '0 0 * * *'
  workflow_dispatch:

jobs:
  send_message:
    runs-on: ubuntu-latest
    name: send morning to your girlfriend

    steps:
    - name: checkout
      uses: actions/checkout@v3
      with:
        ref: master

    - name: sender
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
        architecture: 'x64'
    - run: pip install -r ./requirements.txt && python ./main.py

    env:
      APP_ID: ${{ wx1abc5d4e516e738f }}
      APP_SECRET: ${{ 2d1dfc350af382836fb8e4f147174bcd }}
      TEMPLATE_ID: ${{ qbfqBoP4LJd7CWHvWW3bOiYVR-J7dlobQrTep604K7U }}
      USER_ID: ${{ obK3o64s-x7UyKUueQmk5WK2dNyw }}
      START_DATE: ${{ 2024-06-06 }}
      BIRTHDAY: ${{ 01-02 }}
      CITY: ${{ 长春 }}
